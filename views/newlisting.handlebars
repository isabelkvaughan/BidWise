{{!-- <div class="row">
  <aside>
    {{#if logged_in}}
      <a href="/newlisting" class="login-link">Create New Listing</a>
    {{else}}
    {{/if}}
  </aside>
  <main>
    <div class="col-auto">
      <h2>Welcome, {{}}!</h2>
    </div>

    <section class="new-listing">
      <form>
        <label for="title">Title:</label>
        <input type="text" id="title" name="title" required />

        <label for="description">Description:</label>
        <textarea id="description" name="description" required></textarea>

        <label for="startingPrice">Starting Price:</label>
        <input type="number" id="startingPrice" name="startingPrice" required />

        <label for="endDate">End Date:</label>
        <input type="date" id="endDate" name="endDate" required />

        <button type="submit" id="create-listing-btn">Create Listing</button>
      </form>
    </section>
  </main>
</div>
<script src="js/create-listing-btn.js"></script> --}}